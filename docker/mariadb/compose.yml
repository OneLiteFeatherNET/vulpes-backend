services:
  mariadb:
    image: mariadb:latest
    container_name: mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: vulpes
    ports:
    - "3306:3306"
    volumes:
    - ./mariadb_data:/var/lib/mysql